// src/knowledgeGraph.js

function buildGraph(courses, careers) {
    // Dummy implementation
    return { nodes: [], edges: [] };
  }
  
  function findCareerPaths(graphData, courseId) {
    // Dummy implementation
    return [];
  }
  
  module.exports = { buildGraph, findCareerPaths };
  